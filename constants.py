TOPREWARD_LOGPROB_TIMEOUT_SECS = 5.0  # delay between log_prob_true() calls to avoid rate limits
GVL_GENERATE_TIMEOUT_SECS = 5.0  # delay before generate() call in GVL to avoid rate limits
NUM_FRAMES_DEFAULT = 5  # number of prefix endpoints K for prefix sampling
